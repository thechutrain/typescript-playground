{
	"name": "typescript-security-server",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"engines": {
		"node": ">=10.14.1",
		"npm": ">=6.4.1"
	},
	"scripts": {
		"start": "npm run prod",
		"dev": "cross-env NODE_ENV=development npm run build-tsc && concurrently \"tsc -w\" \"nodemon dist/src/index.js\"",
		"prod": "cross-env NODE_ENV=production npm run build-tsc && node dist/src/index.js",
		"heroku-postbuild": "npm run build-tsc",
		"deploy:heroku": "node dist/src/index.js",
		"rm": "rm -rf dist/",
		"test": "echo \"Error: no test specified\" && exit 1",
		"format": "prettier --write \"src/**\"",
		"tslint": "tslint -c tslint.json -p tsconfig.json",
		"tslint:fix": "tslint -c tslint.json -p tsconfig.json --fix",
		"build-tsc": "tsc",
		"watch-tsc": "tsc -w"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"husky": {
		"hooks": {
			"pre-commit": "lint-staged"
		}
	},
	"lint-staged": {
		"src/*.ts": [
			"npm run format",
			"npm run tslint:fix",
			"git add"
		]
	},
	"dependencies": {},
	"devDependencies": {
		"@types/node": "^10.12.11",
		"concurrently": "^4.1.0",
		"cross-env": "^5.2.0",
		"eslint": "^5.9.0",
		"husky": "^1.2.0",
		"lint-staged": "^8.1.0",
		"nodemon": "^1.18.7",
		"prettier": "^1.15.2",
		"tslint": "^5.11.0",
		"tslint-config-prettier": "^1.17.0",
		"tslint-eslint-rules": "^5.4.0",
		"typescript": "^3.2.1"
	}
}
